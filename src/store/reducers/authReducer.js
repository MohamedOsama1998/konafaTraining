const initialState = {
  signInErr: null,
  signUpErr: null
};

const authReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReducer;
